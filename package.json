{
  "name": "shader-playground",
  "version": "0.0.0",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && vite build",
    "prepublish": "pnpm run build"
  },
  "devDependencies": {
    "@types/node": "^20.10.7",
    "@types/three": "^0.160.0",
    "typescript": "^5.2.2",
    "vite": "^5.0.8",
    "vite-plugin-dts": "^3.7.0"
  },
  "dependencies": {
    "three": "^0.160.0"
  }
}
